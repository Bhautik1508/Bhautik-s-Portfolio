/**
 * Re-export all config values from the canonical config/site.ts
 * so consumers can import from "lib/config" if preferred.
 */
export {
  OPEN_TO_WORK,
  RESUME_URL,
  SITE_TITLE,
  SOCIAL_LINKS,
  siteConfig,
} from "../config/site";
